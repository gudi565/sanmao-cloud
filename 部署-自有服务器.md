# 三猫云 · 自有服务器(VPS)部署指南

> 适合:想自己租服务器长期跑、要掌控、或国内访问的需求。Next.js + SQLite 在 VPS 上免迁移,直接 Docker 起。

---

## 一、租哪台服务器

**规格**:2 核 / 2G 内存起步(Next 生产构建吃内存,1G 会 OOM;运行期只占 300–500M,2G 足够小流量)。系统选 **Ubuntu 22.04 LTS**。

**地区(关键,关系到备案)**:

| 选项 | 适合 | 备案 | 备注 |
|---|---|---|---|
| **香港/海外**(阿里云轻量香港、腾讯云轻量香港、Vultr/Hetzner/DigitalOcean) | **初期快速上线、不想备案** | **不需要** | 可直接绑域名 + HTTPS。CN 访问选带"CN 优化/CN2"线路的 |
| **国内**(阿里云 ECS、腾讯云 CVM) | 正式面向中国大陆用户长期运营 | **必须 ICP 备案**(个人/企业,1–2 周) | 备案后才能用 80/443 + 域名对外;备案期间可用 IP:端口 |

**建议**:先租一台**香港轻量**(便宜、免备案、可立刻上线带域名 HTTPS),跑顺了再决定要不要国内备案迁大陆。

带宽:海外选 30Mbps+ 或按流量计费;图片/动效不多,流量小。

---

## 二、部署(Docker,4 条命令)

SSH 登录服务器后:

```bash
# 1. 装 Docker(Ubuntu,一键)
curl -fsSL https://get.docker.com | sh

# 2. 拉代码
git clone https://github.com/gudi565/sanmao-cloud.git
cd sanmao-cloud

# 3. 建环境变量
cp .env.example .env
nano .env   # 填:AUTH_SECRET(下面给)、EMAIL_*、SMS_*;DATABASE_URL 保持 file:./dev.db
#   AUTH_SECRET 用: openssl rand -base64 48

# 4. 起服务(首次会 build + 首启建表,约 3–6 分钟)
docker compose up -d --build
```

看到容器 `started` 后:
- **临时访问**(无 nginx):把 `docker-compose.yml` 里 ports 改成 `"0.0.0.0:80:3000"` 再 `docker compose up -d`,用 `http://服务器IP` 直接打开。
- **正式访问**(域名 + HTTPS):走下面的 nginx。

查看日志:`docker compose logs -f web`
更新代码:`git pull && docker compose up -d --build`

---

## 三、域名 + HTTPS(nginx 反代)

```bash
# 装 nginx + certbot
sudo apt install -y nginx certbot python3-certbot-nginx

# 站点配置(把 sanmao.com 换成你的域名)
sudo tee /etc/nginx/sites-available/sanmao >/dev/null <<'EOF'
server {
    server_name sanmao.com;            # ← 换成你的域名

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    client_max_body_size 10m;
}
EOF

sudo ln -s /etc/nginx/sites-available/sanmao /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx

# 自动签 HTTPS 证书(Let's Encrypt 免费,会自动改 nginx 配置加 443)
sudo certbot --nginx -d sanmao.com
```

完成后 `https://sanmao.com` 就是你的站。证书到期 certbot 会自动续。

---

## 四、几个要点

- **SQLite 持久化**:`dev.db` 落在宿主 `./data/dev.db`(docker-compose 挂的卷),重建容器不丢,只有删整个项目目录才丢。正式运营量大时建议迁 Postgres。
- **备份**:定期 `cp ./data/dev.db ./data/dev.db.bak` 即可(SQLite 整文件备份)。
- **验证码**:`.env` 里 EMAIL_*/SMS_* 不填 → 验证码打印到 `docker compose logs web`(自己测试用);**开放注册前必须配 SMTP**(QQ/163 邮箱授权码)或阿里云短信。
- **端口**:阿里云/腾讯云安全组要放行 80、443(国内备案后),以及临时测试用的 SSH(22)。香港轻量一般默认放行。
- **OOM**:2G 内存构建时可能紧张,若 `npm run build` 被 kill,临时加 swap:`sudo fallocate -l 2G /swapfile && sudo mkswap /swapfile && sudo swapon /swapfile`。
- **为什么不需要改数据库**:SQLite 是文件库,VPS 本地磁盘就是持久存储,这是 VPS 相比 serverless(Vercel)的最大优势。

---

## 五、文件清单(已随仓库)
- `Dockerfile` — 多阶段构建(Node 20 + Prisma + Next)
- `docker-compose.yml` — 一键起,含 SQLite 持久卷、重启策略
- `.dockerignore` — 不把 node_modules / .next / .env 打进镜像
- `railway.json` — 如果你改主意用 Railway,也还在

**遇到报错**:把 `docker compose logs web` 最后几十行贴回来,我帮你定位。
